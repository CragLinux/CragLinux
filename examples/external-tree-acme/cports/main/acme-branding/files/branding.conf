# ACME product branding (docs/08 §7) — configuration shipped via the
# acme-branding PACKAGE (contrast with ../../../overlay/etc/acme/
# defaults.toml, which ships via the tree overlay). Both are plain
# config and pass the code/config fence either way.
product = ACME Gateway
support = https://support.acme.example
