# Astro U-Boot Kconfig fragment for rpi_arm64_defconfig (docs/04 §4).
#
# Same AD-009 deviation as the QEMU boards (MIGRATION-NOTES §12): env in
# a FAT file (uboot.env) on the bootenv partition — here mmc 0 part 2
# (the SD card) instead of virtio. fw_env.config on the userspace side
# points libubootenv at the same file via the bootenv mount
# (rootfs.sh reads CONFIG_ENV_SIZE from this fragment).
CONFIG_ENV_IS_IN_FAT=y
# CONFIG_ENV_IS_NOWHERE is not set
CONFIG_ENV_FAT_INTERFACE="mmc"
# mmc 1, NOT 0 (first-metal-boot finding): rpi_arm64 U-Boot enumerates
# mmc@7e300000 (the SDIO bus wired to the CYW43455 wifi chip) as mmc 0
# and the actual SD slot, mmc@7e340000 (emmc2), as mmc 1. mmc 0 answers
# every access with "Card did not respond to voltage select! : -110".
CONFIG_ENV_FAT_DEVICE_AND_PART="1:2"
CONFIG_ENV_FAT_FILE="uboot.env"
CONFIG_ENV_SIZE=0x10000
CONFIG_FAT_WRITE=y
# Slot logic needs `part number` (PARTLABEL lookup) + script bootmeth
CONFIG_CMD_PART=y
CONFIG_BOOTMETH_SCRIPT=y
CONFIG_CMD_SAVEENV=y
# Host tools without OpenSSL (Fedora 44 drops the ENGINE headers U-Boot's
# signing tools include); boot.scr is compiled by the container's mkimage.
# CONFIG_TOOLS_LIBCRYPTO is not set
# CONFIG_TOOLS_FIT_SIGNATURE is not set
# CONFIG_TOOLS_KWBIMAGE is not set
