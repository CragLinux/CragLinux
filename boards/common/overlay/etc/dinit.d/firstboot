# Crag: firstboot oneshot (docs/07 §3, docs/02 §5.1 graph node) — API
# token + initial crag.json, once per /data lifetime (factory reset
# wipes /data, so it reruns after one). No-op on direct-boot dev disks.
# Ordered before cragd so the token and store exist when the daemon
# starts reading them (M3).

type = scripted
command = /usr/lib/crag/firstboot.sh
depends-on: data-mount
before: cragd
